//console.log(); // global object
// setTimeout();
// clearTimeout();
// setInterval();
// clearInterval();

var message = '';
// window.message can be accessed in browser
// not available in nodejs
// for not it is:

console.log(global.message); // not available in global 
